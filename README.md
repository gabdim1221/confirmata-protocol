# Tessera Protocol

The open standard for cryptographic attestation of human acts. Apache 2.0.

The full specification lives in `spec/v0.1/tessera.md`. Test vectors that any conformant implementation must pass are in `test-vectors/`.

This protocol is implemented by [Confirmata](https://confirmata.io) — the reference deployment of the `authorship` Tessera type.
